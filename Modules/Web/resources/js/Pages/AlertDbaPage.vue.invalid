<script setup>
import { Head } from '@inertiajs/vue3'
import AppLayout from '../../Layouts/AppLayout.vue'

defineOptions({ layout: AppLayout })

defineProps({
    alerts: {
        type: Array,
        default: () => [],
    },
})
</script>

<template>
    <Head title="DBA Alert" />

    <div class="p-6">
        <div class="mb-4">
            <h1 class="text-xl font-semibold text-gray-800">DBA Alert</h1>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white shadow-sm">
            <div v-if="alerts.length === 0" class="p-6 text-center text-sm text-gray-500">
                No DBA alerts to display.
            </div>

            <ul v-else class="divide-y divide-gray-200">
                <li
                    v-for="(alert, index) in alerts"
                    :key="index"
                    class="p-4 text-sm text-gray-700"
                >
                    {{ alert }}
                </li>
            </ul>
        </div>
    </div>
</template>